<form method="post" action="{{ path('app_vol_delete', {'id': vol.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce vol ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ vol.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
