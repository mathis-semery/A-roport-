{% extends 'base.html.twig' %}

{% block title %}Aéroport International de Dugny | Voyages d'exception{% endblock %}

{% block stylesheets %}
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css"/>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --primary: #005f96;
            --secondary: #ff7f00;
            --dark: #1a2a36;
            --light: #f8f9fa;
        }

        body {
            font-family: 'Montserrat', sans-serif;
            overflow-x: hidden;
        }

        /* Hero Section */
        .hero {
            background: linear-gradient(rgba(0, 0, 0, 0.7), rgba(0, 0, 0, 0.7)), url('https://images.unsplash.com/photo-1436491865332-7a61a109cc05?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=2070&q=80');
            background-size: cover;
            background-position: center;
            height: 100vh;
            min-height: 600px;
            display: flex;
            align-items: center;
            position: relative;
        }

        .hero-content {
            z-index: 2;
        }

        .hero h1 {
            font-size: clamp(2.5rem, 5vw, 4.5rem);
            font-weight: 800;
            line-height: 1.2;
            text-shadow: 2px 2px 8px rgba(0,0,0,0.5);
        }

        .hero p {
            font-size: clamp(1rem, 2vw, 1.5rem);
            max-width: 700px;
            margin: 0 auto 2rem;
            text-shadow: 1px 1px 4px rgba(0,0,0,0.5);
        }

        /* Buttons */
        .btn-primary-custom {
            background-color: var(--primary);
            border: none;
            padding: 12px 30px;
            font-weight: 600;
            letter-spacing: 0.5px;
            transition: all 0.3s ease;
            border-radius: 30px;
        }

        .btn-primary-custom:hover {
            background-color: #004a75;
            transform: translateY(-3px);
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.2);
        }

        .btn-secondary-custom {
            background-color: var(--secondary);
            color: white;
        }

        /* Section Styling */
        .section {
            padding: 100px 0;
        }

        .section-title {
            position: relative;
            display: inline-block;
            margin-bottom: 60px;
            font-weight: 700;
            color: var(--dark);
        }

        .section-title:after {
            content: '';
            position: absolute;
            width: 60%;
            height: 4px;
            background: var(--secondary);
            bottom: -15px;
            left: 20%;
            border-radius: 2px;
        }

        /* Cards */
        .card {
            border: none;
            border-radius: 15px;
            overflow: hidden;
            transition: all 0.4s cubic-bezier(0.175, 0.885, 0, 1);
            box-shadow: 0 6px 18px rgba(0,0,0,0.1);
            height: 100%;
        }

        .card:hover {
            transform: translateY(-10px);
            box-shadow: 0 20px 40px rgba(0,0,0,0.15);
        }

        .card-img-top {
            height: 220px;
            object-fit: cover;
            transition: transform 0.5s ease;
        }

        .card:hover .card-img-top {
            transform: scale(1.05);
        }

        .card-body {
            padding: 25px;
        }

        .card-title {
            font-weight: 700;
            margin-bottom: 15px;
            color: var(--dark);
        }

        /* Features */
        .feature-icon {
            font-size: 2.5rem;
            color: var(--primary);
            margin-bottom: 20px;
        }

        /* Testimonials */
        .testimonial-card {
            background: white;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.05);
        }

        .testimonial-img {
            width: 80px;
            height: 80px;
            border-radius: 50%;
            object-fit: cover;
            border: 4px solid var(--primary);
        }

        /* Newsletter */
        .newsletter {
            background: linear-gradient(135deg, var(--primary) 0%, #003d66 100%);
            border-radius: 15px;
            padding: 50px;
            color: white;
        }

        /* Footer */
        footer {
            background-color: var(--dark);
            color: white;
            padding: 70px 0 0;
        }

        .footer-links h5 {
            font-weight: 700;
            margin-bottom: 25px;
            position: relative;
        }

        .footer-links h5:after {
            content: '';
            position: absolute;
            width: 40px;
            height: 3px;
            background: var(--secondary);
            bottom: -10px;
            left: 0;
        }

        .footer-links a {
            color: #adb5bd;
            display: block;
            margin-bottom: 10px;
            transition: all 0.3s ease;
            text-decoration: none;
        }

        .footer-links a:hover {
            color: white;
            padding-left: 5px;
        }

        .social-icons a {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            width: 40px;
            height: 40px;
            background: rgba(255,255,255,0.1);
            border-radius: 50%;
            margin-right: 10px;
            transition: all 0.3s ease;
        }

        .social-icons a:hover {
            background: var(--secondary);
            transform: translateY(-3px);
        }

        /* Animations */
        .fade-in {
            animation: fadeIn 1s ease-in-out;
        }

        .delay-1 {
            animation-delay: 0.2s;
        }

        .delay-2 {
            animation-delay: 0.4s;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        /* Responsive Adjustments */
        @media (max-width: 768px) {
            .section {
                padding: 60px 0;
            }

            .hero {
                height: auto;
                padding: 120px 0;
            }
        }
    </style>
{% endblock %}

{% block body %}
    <!-- Hero Section -->
    <section class="hero text-center text-white">
        <div class="container hero-content">
            <div class="row justify-content-center">
                <div class="col-lg-10">
                    <h1 class="animate__animated animate__fadeInDown mb-4">Explorez le monde avec l'Aéroport de Dugny</h1>
                    <p class="animate__animated animate__fadeIn animate__delay-1s mb-5">Découvrez plus de 120 destinations à travers le globe avec un service d'exception</p>
                    <div class="animate__animated animate__fadeIn animate__delay-2s">
                        <a href="{{ path('app_utilisateur_new') }}" class="btn btn-primary-custom btn-lg me-3"><i class="fas fa-plane me-2"></i>Réserver un vol</a>
                        <a href="{{ path('app_vol_destination') }}" class="btn btn-outline-light btn-lg"><i class="fas fa-map-marked-alt me-2"></i>Nos destinations</a>



                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Features Section -->
    <section class="section bg-light">
        <div class="container">
            <h2 class="section-title text-center">Pourquoi choisir Dugny ?</h2>

            <div class="row g-4">
                <div class="col-md-4">
                    <div class="text-center p-4 h-100 fade-in">
                        <div class="feature-icon">
                            <i class="fas fa-shield-alt"></i>
                        </div>
                        <h3>Sécurité maximale</h3>
                        <p class="text-muted">Notre aéroport répond aux normes de sécurité les plus strictes avec un contrôle renforcé.</p>
                    </div>
                </div>

                <div class="col-md-4">
                    <div class="text-center p-4 h-100 fade-in delay-1">
                        <div class="feature-icon">
                            <i class="fas fa-clock"></i>
                        </div>
                        <h3>Ponctualité</h3>
                        <p class="text-muted">95% de nos vols partent à l'heure grâce à notre gestion optimisée des flux.</p>
                    </div>
                </div>

                <div class="col-md-4">
                    <div class="text-center p-4 h-100 fade-in delay-2">
                        <div class="feature-icon">
                            <i class="fas fa-concierge-bell"></i>
                        </div>
                        <h3>Service premium</h3>
                        <p class="text-muted">Un accueil personnalisé et des services haut de gamme pour votre confort.</p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Destinations Section -->
    <section id="destinations" class="section">
        <div class="container">
            <h2 class="section-title text-center">Nos destinations phares</h2>
            <p class="text-center text-muted mb-5">Découvrez nos vols les plus populaires vers des villes extraordinaires</p>

            <div class="row g-4">
                {% set destinations = [
                    {
                        'city': 'Paris',
                        'country': 'France',
                        'price': 'À partir de 199€',
                        'desc': 'La ville lumière vous attend avec ses monuments iconiques et sa gastronomie raffinée.',
                        'img': 'https://images.unsplash.com/photo-1431274172761-fca41d930114?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=2070&q=80'
                    },
                    {
                        'city': 'New York',
                        'country': 'États-Unis',
                        'price': 'À partir de 599€',
                        'desc': 'Découvrez la ville qui ne dort jamais avec ses gratte-ciels impressionnants.',
                        'img': 'https://images.unsplash.com/photo-1485871981521-5b1fd3805eee?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=2070&q=80'
                    },
                    {
                        'city': 'Tokyo',
                        'country': 'Japon',
                        'price': 'À partir de 899€',
                        'desc': 'Plongez dans la culture japonaise entre tradition et ultra-modernité.',
                        'img': 'https://images.unsplash.com/photo-1542051841857-5f90071e7989?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=2070&q=80'
                    }
                ] %}

                {% for destination in destinations %}
                    <div class="col-lg-4 col-md-6">
                        <div class="card fade-in" style="animation-delay: {{ 0.2 * loop.index }}s">
                            <img src="{{ destination.img }}" class="card-img-top" alt="{{ destination.city }}">
                            <div class="card-body">
                                <div class="d-flex justify-content-between align-items-center mb-3">
                                    <h5 class="card-title mb-0">{{ destination.city }}</h5>
                                    <span class="badge bg-primary">{{ destination.country }}</span>
                                </div>
                                <p class="card-text text-muted">{{ destination.desc }}</p>
                                <div class="d-flex justify-content-between align-items-center mt-4">
                                    <span class="text-primary fw-bold">{{ destination.price }}</span>
                                    <a href="#" class="btn btn-sm btn-primary-custom">Voir les vols</a>
                                </div>
                            </div>
                        </div>
                    </div>
                {% endfor %}
            </div>

            <div class="text-center mt-5 fade-in">
                <a href="#" class="btn btn-outline-primary">Voir toutes nos destinations <i class="fas fa-arrow-right ms-2"></i></a>
            </div>
        </div>
    </section>

    <!-- Stats Section -->
    <section class="py-5 bg-primary text-white">
        <div class="container">
            <div class="row text-center">
                <div class="col-md-3">
                    <div class="display-4 fw-bold">120+</div>
                    <p class="mb-0">Destinations</p>
                </div>
                <div class="col-md-3">
                    <div class="display-4 fw-bold">50K+</div>
                    <p class="mb-0">Passagers/mois</p>
                </div>
                <div class="col-md-3">
                    <div class="display-4 fw-bold">95%</div>
                    <p class="mb-0">Vols à l'heure</p>
                </div>
                <div class="col-md-3">
                    <div class="display-4 fw-bold">24/7</div>
                    <p class="mb-0">Service client</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Testimonials -->
    <section class="section bg-light">
        <div class="container">
            <h2 class="section-title text-center">Ils nous font confiance</h2>

            <div class="row g-4">
                {% set testimonials = [
                    {
                        'name': 'Sophie Martin',
                        'role': 'Voyageuse fréquente',
                        'text': 'Jamais déçue par le service de Dugny Airport. Tout est fluide du check-in à l embarquement.',
                        'img': 'https://randomuser.me/api/portraits/women/32.jpg'
                    },
                    {
                        'name': 'Thomas Leroy',
                        'role': 'Homme d affaires',
                        'text': 'La lounge VIP est exceptionnelle. Je prends toujours mes vols depuis Dugny pour mon confort.',
                        'img': 'https://randomuser.me/api/portraits/men/75.jpg'
                    },
                    {
                        'name': 'Camille Dubois',
                        'role': 'Famille voyageuse',
                        'text': 'Aire de jeux pour enfants super bien pensée. Mes enfants adorent cet aéroport !',
                        'img': 'https://randomuser.me/api/portraits/women/63.jpg'
                    }
                ] %}

                {% for testimonial in testimonials %}
                    <div class="col-md-4">
                        <div class="testimonial-card h-100 fade-in" style="animation-delay: {{ 0.2 * loop.index }}s">
                            <div class="text-center mb-4">
                                <img src="{{ testimonial.img }}" class="testimonial-img" alt="{{ testimonial.name }}">
                            </div>
                            <p class="text-muted mb-4"><i class="fas fa-quote-left text-primary me-2"></i>{{ testimonial.text }}<i class="fas fa-quote-right text-primary ms-2"></i></p>
                            <div class="text-center">
                                <h5 class="mb-1">{{ testimonial.name }}</h5>
                                <small class="text-muted">{{ testimonial.role }}</small>
                            </div>
                        </div>
                    </div>
                {% endfor %}
            </div>
        </div>
    </section>

    <!-- Newsletter -->
    <section class="section">
        <div class="container">
            <div class="newsletter fade-in">
                <div class="row align-items-center">
                    <div class="col-lg-6 mb-4 mb-lg-0">
                        <h3 class="text-white mb-3">Abonnez-vous à notre newsletter</h3>
                        <p class="text-white-50">Recevez nos offres exclusives et actualités directement dans votre boîte mail.</p>
                    </div>
                    <div class="col-lg-6">
                        <form class="row g-2">
                            <div class="col-8">
                                <input type="email" class="form-control form-control-lg" placeholder="Votre email">
                            </div>
                            <div class="col-4">
                                <button type="submit" class="btn btn-secondary-custom btn-lg w-100">S'abonner</button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer>
        <div class="container">
            <div class="row g-4">
                <div class="col-lg-4">
                    <h5 class="text-white mb-4">Aéroport de Dugny</h5>
                    <p class="text-white">Leader dans le transport aérien depuis 1985, nous nous engageons à offrir une expérience de voyage exceptionnelle.</p>
                    <div class="social-icons mt-4">
                        <a href="#"><i class="fab fa-facebook-f"></i></a>
                        <a href="#"><i class="fab fa-twitter"></i></a>
                        <a href="#"><i class="fab fa-instagram"></i></a>
                        <a href="#"><i class="fab fa-linkedin-in"></i></a>
                    </div>
                </div>

                <div class="col-lg-2 col-md-4">
                    <div class="footer-links">
                        <h5>Compagnies</h5>
                        <a href="#">Air France</a>
                        <a href="#">Lufthansa</a>
                        <a href="#">Emirates</a>
                        <a href="#">Qatar Airways</a>
                        <a href="#">Delta</a>
                    </div>
                </div>

                <div class="col-lg-2 col-md-4">
                    <div class="footer-links">
                        <h5>Services</h5>
                        <a href="#">Réservation</a>
                        <a href="#">Check-in</a>
                        <a href="#">Bagages</a>
                        <a href="#">Assistance</a>
                        <a href="#">VIP Lounge</a>
                    </div>
                </div>

                <div class="col-lg-2 col-md-4">
                    <div class="footer-links">
                        <h5>Informations</h5>
                        <a href="#">À propos</a>
                        <a href="#">Contact</a>
                        <a href="#">Carrières</a>
                        <a href="#">Presse</a>
                        <a href="#">CGU</a>
                    </div>
                </div>

                <div class="col-lg-2">
                    <div class="footer-links">
                        <h5>Contact</h5>
                        <p class="text-muted-white mb-2"><i class="fas fa-map-marker-alt me-2"></i> Dugny, France</p>
                        <p class="text-muted-white mb-2"><i class="fas fa-phone me-2"></i> +33 1 23 45 67 89</p>
                        <p class="text-muted-white mb-2"><i class="fas fa-envelope me-2"></i> contact@dugny-airport.com</p>
                    </div>
                </div>
            </div>

            <hr class="my-5 bg-secondary">

            <div class="row">
                <div class="col-md-6 text-center text-md-start">
                    <p class="mb-0">&copy; {{ "now"|date("Y") }} Aéroport International de Dugny. Tous droits réservés.</p>
                </div>
                <div class="col-md-6 text-center text-md-end">
                    <div class="d-flex justify-content-md-end justify-content-center">
                        <a href="#" class="text-white-50 me-3">Confidentialité</a>
                        <a href="#" class="text-white-50 me-3">Cookies</a>
                        <a href="#" class="text-white-50">Mentions légales</a>
                    </div>
                </div>
            </div>
        </div>
    </footer>
{% endblock %}